---
server:
  port: ${PORT:8010}
  servlet:
    context-path: /rest
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8761/eureka}
    instance:
      prefereIpAddress: true
auth:
  jwt:
    key:
      public: ${KBE_JWT_PUBLIC_KEY}
