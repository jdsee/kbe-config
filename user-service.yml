---
server:
  port: ${PORT:8009}
  servlet:
    context-path: /rest
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8761/eureka}
    instance:
      prefereIpAddress: true
security:
  jwt:
    key:
      public:
